server:
  port: 8082

.env:
  directory: .
  filename: .env
  prefix: ""

spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
  config:
    import: optional:file:.env[.properties]
  data:
    mongodb:
      uri: ${URI}
      database: FilmesApollo70
  rabbitmq:
    host: localhost
    port: 5672
    username: rabbitmq
    password: rabbitmq